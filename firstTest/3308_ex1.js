/**
 * 다음 조건을 만족하는 변수 3개를 선언하여라.
 *
 * 1. 선언 이전에 코드에서 사용할 수 없고, 선언 이후 값을 변경할 수 있는 변수 v1. 초기 값은 1.
 * 2. 선언 이전에 코드에서 사용할 수 있고, 선언 이후 값을 변경할 수 있는 변수 v2. 초기 값은 2.
 * 3. 선언 이전에 코드에서 사용할 수 없고, 선언 이후 값을 변경할 수 없는 변수 v3. 초기 값은 3.
 */

let v1 = 1;
var v2 = 2;
const v3 = 3;